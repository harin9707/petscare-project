<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.test.dto.PetDTO">
	
	<select id="listItsPets" parameterType="java.lang.Integer" resultType="com.test.dto.PetDTO">
    	select * from pet_info where cid = #{ value }
  	</select>
  	
	<!-- 세미콜론 없이 쿼리문 작성! -->
	
</mapper>